// Two-row header: top utility row (icons only) + main row (logo/nav left, search right).
// NavDropdown anchors itself right below the full stack using HEADER_HEIGHT.
export const TOP_ROW_HEIGHT = 44
export const MAIN_ROW_HEIGHT = 116
export const HEADER_HEIGHT = TOP_ROW_HEIGHT + MAIN_ROW_HEIGHT
