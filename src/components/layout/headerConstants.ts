// Shared row heights for the 3-row header (logo / nav / search+utility).
// NavDropdown anchors itself right below the full stack using HEADER_HEIGHT.
export const LOGO_ROW_HEIGHT = 96
export const NAV_ROW_HEIGHT = 52
export const UTIL_ROW_HEIGHT = 64
export const HEADER_HEIGHT = LOGO_ROW_HEIGHT + NAV_ROW_HEIGHT + UTIL_ROW_HEIGHT
